{% extends 'base-back.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fournisseur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fournisseur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fournisseur_index') }}">Retour a liste   |</a>

    <a href="{{ path('app_fournisseur_edit', {'id': fournisseur.id}) }}">Mise a jour </a>

    {{ include('fournisseur/_delete_form.html.twig') }}
{% endblock %}
