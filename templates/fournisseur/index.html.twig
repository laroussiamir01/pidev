{% extends 'back.html.twig' %}

{% block title %}Fournisseur {% endblock %}

{% block body %}
    <h1>List des Fournisseurs </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fournisseur in fournisseurs %}
            <tr>
                <td>{{ fournisseur.id }}</td>
                <td>{{ fournisseur.nom }}</td>
                <td>
                    <a href="{{ path('app_fournisseur_show', {'id': fournisseur.id}) }}">Afficher  |</a>
                    <a href="{{ path('app_fournisseur_edit', {'id': fournisseur.id}) }}">Mise a jour  |</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_fournisseur_new') }}">Ajouter un nouveau fournisseur</a>
{% endblock %}
