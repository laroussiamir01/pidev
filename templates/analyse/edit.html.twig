{% extends 'back.html.twig' %}

{% block edit %}
    <h1>Edit Analyse</h1>

    {{ include('analyse/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_analyse_index') }}">back to list</a>

    {{ include('analyse/_delete_form.html.twig') }}
{% endblock %}
