<form method="post" action="{{ path('app_materiel_controller_back_delete', {'id_mat': materiel.id_mat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel.id_mat) }}">
    <button class="btn">Delete</button>
</form>
