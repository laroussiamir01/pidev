{% extends 'back.html.twig' %}

{% block title %}Favoris{% endblock %}

{% block fav %}
    <h1>Favoris</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favori.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ favori.note }}</td>
            </tr>
        </tbody>
    </table>

 

    {{ include('favoris/_delete_form.html.twig') }}
{% endblock %}
